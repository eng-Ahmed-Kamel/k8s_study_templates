ReadWriteOnce (rwo)>>>> this mode allow the volume to be mounted as read and write on one node
ReadWriteMany (rwx)>>>> this mode allow the volume to be mounted as read and write in multi node
ReadOnlyMany (rox)>>>> this mode allow the volume to be mounted as read  in multi node

in pv only one access mode
hostpath >> rwo
awsElasticBlockStore >> rwo
azurefile>> rwo,rox,rwx


kubectl get pv
kubectl describe pv <pv-name>
kubectl delete pv <pv-name>

kubectl get pvc
kubectl describe pvc <pvc-name>
kubectl delete pvc <pvc-name>


CSI  >container storage interface is plugin

some storage type supported in kubernetes 
hostpath,iscsi,local,nfs,amazonEBS,azuredisk,azurefile

storageclass:
static ,dynamic

static life cycle create volume manually >>> pv>> pvc >>>> pod
dynamic life cycle sc >>>>pvc>>> pod>>> then sc>>pv on cloud and connect pv with pvc


dynamic >> create pvc automatic
ReclaimPolicy:  #Delete or Retain
volumeBindingMode: WaitForFirstConsumer (most plugin dont support)or immediate (default)

kubectl get sc
kubectl describe sc <sc-name>
kubectl delete sc <sc-name>


lab

create user with permission "amazonEBSCSIDriverPolicy "  >>>>> create access key
create i am role with permission "amazonEBSCSIDriverPolicy attach to ec2
install csi-ebs driver in cluster 
add user credinaitals in cluster  

kubectl create secret generic <secret name --aws-secret> --namespace kube-system --from-literal "key_id=<...........................>" --from-literal "access_key=<...........................>"